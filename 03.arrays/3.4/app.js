//Write a program that will display the minimum and maximum elements of a given array.

const randomD20Array = [9, 4, 2, 7, 3] //this adventurer has shit luck and i wouldn't want to be in their party
const min = Math.min(...randomD20Array);
const max = Math.max(...randomD20Array);

console.log(min);
console.log(max);